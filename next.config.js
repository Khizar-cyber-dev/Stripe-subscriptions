/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // 'appDir' is not needed anymore in Next.js 13+
    // Remove it unless you're on an older version
  },
  // 'swcMinify' is no longer needed; it's default in Next 13+
}

module.exports = nextConfig;
